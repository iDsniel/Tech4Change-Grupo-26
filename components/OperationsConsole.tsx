"use client";
import { useState } from "react";
import type { HysterData } from "@/lib/hyster";
import { followUp, inputSummary, statusLabels, validateInput, validateOrder, type DailyInput, type WorkOrder, type Workspace, type OrderStatus } from "@/lib/operations";

export type OperationsTab = "overview" | "cards" | "maintenance" | "inputs" | "legacy";
const f = (n: number | null | undefined, suffix = "") => n == null ? "Não disponível" : n.toLocaleString("pt-BR", { maximumFractionDigits: 2 }) + suffix;
const today = () => new Date().toLocaleDateString("en-CA");
const metricLabels: Record<string, string> = { uses: "Partidas / usos", distanceKm: "Distância (km)", keyHours: "Chave ligada (h)", presenceHours: "Presença (h)", motionHours: "Movimento (h)", hydraulicHours: "Hidráulica (h)", workHours: "Trabalho (h)", liftHours: "Elevação (h)", lowerHours: "Descida (h)", highSpeedHours: "Alta velocidade (h)", reverseHours: "Marcha ré (h)", forwardHours: "Marcha à frente (h)", idleHours: "Ociosidade total (h)", monitoredHours: "Monitorado (h)", hydraulicMeterHours: "Medidor hidráulico (h)", tractionMeterHours: "Medidor tração (h)" };
type Props = { tab: OperationsTab; data: HysterData; workspace: Workspace; save: (w: Workspace) => Promise<void>; busy: boolean; suggestion?: { assetId: string; title: string } };

export default function OperationsConsole({ tab, data, workspace, save, busy, suggestion }: Props) {
  const [asset, setAsset] = useState("all"), [month, setMonth] = useState("all"), [card, setCard] = useState("all");
  const [error, setError] = useState("");
  const [legacyId, setLegacyId] = useState("all");
  const [editing, setEditing] = useState<DailyInput | null>(null);
  const [orderFilter, setOrderFilter] = useState("all");
  const months = [...new Set([...data.daily.map(r => r.date.slice(0, 7)), ...workspace.inputs.map(r => r.date.slice(0, 7))])].sort();
  const matching = (r: { assetId: string; date: string }) => (asset === "all" || r.assetId === asset) && (month === "all" || r.date.startsWith(month));
  async function submitOrder(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); const form = event.currentTarget; const fields = new FormData(form);
    const at = new Date().toISOString();
    try {
      const order = validateOrder({ id: crypto.randomUUID(), assetId: String(fields.get("assetId")), title: String(fields.get("title")), kind: String(fields.get("kind")) as WorkOrder["kind"], priority: String(fields.get("priority")) as WorkOrder["priority"], team: String(fields.get("team")), dueDate: String(fields.get("dueDate")), status: "open", openedAt: at, completedAt: null, note: String(fields.get("note")), history: [{ at, status: "open", note: "Ordem registrada" }] });
      await save({ ...workspace, orders: [...workspace.orders, order] }); setError(""); form.reset();
    } catch (e) { setError((e as Error).message); }
  }
  async function transition(event: React.FormEvent<HTMLFormElement>, order: WorkOrder) {
    event.preventDefault(); const fields = new FormData(event.currentTarget); const status = String(fields.get("status")) as OrderStatus;
    try {
      const note = String(fields.get("note"));
      if (!note.trim()) throw new Error("Descreva a execução ou o motivo da mudança.");
      const updated = validateOrder({ ...order, status, completedAt: status === "completed" ? String(fields.get("completedAt")) : null, history: [...order.history, { at: new Date().toISOString(), status, note }] });
      if (updated.completedAt && updated.completedAt > today()) throw new Error("Conclusão não pode estar no futuro.");
      await save({ ...workspace, orders: workspace.orders.map(o => o.id === order.id ? updated : o) }); setError("");
    } catch (e) { setError((e as Error).message); }
  }
  async function submitInput(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); const fields = new FormData(event.currentTarget);
    const number = (key: string) => fields.get(key) === "" ? null : Number(fields.get(key));
    try {
      const row = validateInput({ id: editing?.id ?? crypto.randomUUID(), assetId: String(fields.get("assetId")), date: String(fields.get("date")), plannedHours: number("plannedHours"), downtimeHours: number("downtimeHours"), fuelQuantity: number("fuelQuantity"), fuelUnit: String(fields.get("fuelUnit")) as "L" | "kg", costBRL: number("costBRL"), production: number("production"), productionUnit: String(fields.get("productionUnit")) as "t" | "movimentos", note: String(fields.get("note")) });
      if (workspace.inputs.some(r => r.assetId === row.assetId && r.date === row.date && r.id !== row.id)) throw new Error("Já existe um apontamento para esse equipamento/dia. Use Editar.");
      await save({ ...workspace, inputs: [...workspace.inputs.filter(r => r.id !== row.id), row] }); setEditing(null); setError("");
    } catch (e) { setError((e as Error).message); }
  }
  const filters = <div className="hysterFilters"><label>Equipamento<select value={asset} onChange={e => setAsset(e.target.value)}><option value="all">Toda a frota</option>{data.assets.map(a => <option key={a.assetId}>{a.assetId}</option>)}</select></label><label>Mês<select value={month} onChange={e => setMonth(e.target.value)}><option value="all">Todos</option>{months.map(m => <option key={m}>{m}</option>)}</select></label></div>;
  const assetField = (value?: string) => <label>Equipamento<select name="assetId" defaultValue={value ?? data.assets[0]?.assetId}>{data.assets.map(a => <option key={a.assetId}>{a.assetId}</option>)}</select></label>;

  if (tab === "cards") {
    const events = data.events.filter(matching).filter(e => card === "all" || (e.cardCode ?? "unknown") === card);
    const codes = [...new Set(data.events.map(e => e.cardCode ?? "unknown"))].sort();
    return <section className="detailCard"><h2>Acompanhamento por cartão</h2><p>O código identifica o cartão registrado no evento. Estes relatórios não fornecem horas trabalhadas por cartão; as horas do equipamento não são atribuídas ao operador.</p>{filters}<label>Cartão<select value={card} onChange={e => setCard(e.target.value)}><option value="all">Todos os cartões</option>{codes.map(c => <option value={c} key={c}>{c === "unknown" ? "Sem código na base" : c}</option>)}</select></label><p>{events.length} registros · {events.filter(e => e.type === "Impacto").length} impactos · {events.filter(e => e.type === "Falha do sistema").length} registros de falha. Associação ao evento não comprova responsabilidade.</p><div className="hysterTable"><table><thead><tr><th>Data/hora</th><th>Equipamento</th><th>Cartão</th><th>Tipo</th><th>Linha de origem</th></tr></thead><tbody>{[...events].sort((a, b) => `${b.date} ${b.time}`.localeCompare(`${a.date} ${a.time}`)).slice(0, 200).map((e, i) => <tr key={`${e.sourceRow}-${i}`}><td>{e.date} {e.time}</td><td>{e.assetId}</td><td>{e.cardCode ?? "Não disponível"}</td><td>{e.type}</td><td>{e.sourceRow}</td></tr>)}</tbody></table></div><p>Exibindo até 200 eventos mais recentes do filtro. Totais consideram todos os registros.</p></section>;
  }
  if (tab === "maintenance") {
    const orders = workspace.orders.filter(o => (asset === "all" || o.assetId === asset) && (orderFilter === "all" || o.status === orderFilter));
    return <><section className="detailCard"><h2>Gestão de operação e manutenção</h2><p>Registre uma investigação, preventiva, corretiva ou melhoria operacional. As ordens são apontamentos da equipe e não são criadas automaticamente a partir de eventos.</p>{error && <p role="alert">{error}</p>}<form className="opsForm" onSubmit={submitOrder} key={suggestion?.title ?? "new"}>{assetField(suggestion?.assetId)}<label>Tipo<select name="kind"><option value="corrective">Corretiva / investigação</option><option value="preventive">Preventiva</option><option value="operational">Melhoria operacional</option></select></label><label>Título<input name="title" required maxLength={200} defaultValue={suggestion?.title} /></label><label>Equipe responsável<input name="team" required placeholder="Ex.: Manutenção mecânica" /></label><label>Prazo<input type="date" name="dueDate" required /></label><label>Prioridade<select name="priority"><option value="normal">Normal</option><option value="high">Alta</option></select></label><label className="opsWide">Contexto e ação proposta<textarea name="note" maxLength={2000} /></label><button disabled={busy}>Registrar ordem</button></form></section>
      <section className="detailCard"><h2>Fila e acompanhamento</h2><div className="hysterFilters"><label>Equipamento<select value={asset} onChange={e => setAsset(e.target.value)}><option value="all">Toda a frota</option>{data.assets.map(a => <option key={a.assetId}>{a.assetId}</option>)}</select></label><label>Situação<select value={orderFilter} onChange={e => setOrderFilter(e.target.value)}><option value="all">Todas</option>{Object.entries(statusLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label></div>{orders.length === 0 && <p>Nenhuma ordem registrada neste filtro.</p>}{orders.map(o => {
        const follow = followUp(data, o); const late = ["open", "in_progress"].includes(o.status) && o.dueDate < today();
        return <article className="opsOrder" key={o.id}><h3>{o.assetId} · {o.title}</h3><p>{statusLabels[o.status]}{late ? " · Prazo vencido" : ""} · {o.priority === "high" ? "Alta prioridade" : "Prioridade normal"} · {o.team} · Prazo: {o.dueDate}</p><p>{o.note}</p><form className="opsForm" onSubmit={e => void transition(e, o)}><label>Situação<select name="status" defaultValue={o.status}>{Object.entries(statusLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label><label>Data real de conclusão<input type="date" name="completedAt" defaultValue={o.completedAt ?? ""} max={today()} /></label><label>Execução / justificativa<input name="note" required maxLength={2000} /></label><button disabled={busy}>Salvar andamento</button></form>{follow && <p><strong>Acompanhamento após a ação:</strong> ociosidade/chave antes {f(follow.before.idlePct, "%")} ({follow.before.records} dias), depois {f(follow.after.idlePct, "%")} ({follow.after.records} dias). {follow.delta === null ? "Aguardando pelo menos 5 registros antes e depois, nas janelas de 14 dias." : `Variação: ${f(follow.delta)} pontos percentuais. Comparação descritiva, sem comprovação causal; valide a demanda.`}</p>}<details><summary>Histórico da ordem</summary>{o.history.map((h, i) => <p key={i}>{h.at} · {statusLabels[h.status]} · {h.note}</p>)}</details></article>;
      })}</section></>;
  }
  if (tab === "inputs") {
    const rows = workspace.inputs.filter(matching), summary = inputSummary(rows);
    return <><section className="detailCard"><h2>Outros dados da operação</h2><p>Apontamento diário por equipamento: calendário, parada dentro do horário planejado, abastecimento, custo e produção. Campo vazio significa não informado; zero deve ser informado explicitamente.</p>{error && <p role="alert">{error}</p>}<form className="opsForm" key={editing?.id ?? `new-${workspace.inputs.length}`} onSubmit={submitInput}>{assetField(editing?.assetId)}<label>Dia<input type="date" name="date" required defaultValue={editing?.date} /></label>{[["plannedHours", "Horas planejadas"], ["downtimeHours", "Horas de parada no período planejado"], ["fuelQuantity", "Quantidade abastecida"], ["costBRL", "Custo realizado (R$)"], ["production", "Volume produzido / movimentado"]].map(([name, label]) => <label key={name}>{label}<input name={name} type="number" min="0" step="0.01" defaultValue={editing?.[name as keyof DailyInput] as number ?? ""} /></label>)}<label>Unidade do combustível<select name="fuelUnit" defaultValue={editing?.fuelUnit ?? "L"}><option>L</option><option>kg</option></select></label><label>Unidade da produção<select name="productionUnit" defaultValue={editing?.productionUnit ?? "t"}><option>t</option><option>movimentos</option></select></label><label className="opsWide">Fonte / observação<textarea name="note" defaultValue={editing?.note} maxLength={2000} /></label><button disabled={busy}>{editing ? "Salvar alteração" : "Registrar apontamento"}</button>{editing && <button type="button" onClick={() => setEditing(null)}>Cancelar edição</button>}</form></section>
      <section className="detailCard"><h2>Indicadores complementares</h2>{filters}<p>Disponibilidade nos {summary.covered} equipamento-dias com planejamento e parada informados: <strong>{f(summary.availability, "%")}</strong>. Não representa dias sem apontamento.</p><p>Abastecimentos informados: {f(summary.fuelL)} L e {f(summary.fuelKg)} kg · Custos informados: R$ {f(summary.cost)} · Produção informada: {f(summary.tonnes)} t e {f(summary.movements)} movimentos.</p><div className="hysterTable"><table><thead><tr><th>Dia</th><th>Ativo</th><th>Planejado</th><th>Parada</th><th>Abastecimento</th><th>Custo R$</th><th>Produção</th><th>Ação</th></tr></thead><tbody>{[...rows].sort((a, b) => b.date.localeCompare(a.date)).map(r => <tr key={r.id}><td>{r.date}</td><td>{r.assetId}</td><td>{f(r.plannedHours, " h")}</td><td>{f(r.downtimeHours, " h")}</td><td>{f(r.fuelQuantity, ` ${r.fuelUnit}`)}</td><td>{f(r.costBRL)}</td><td>{f(r.production, ` ${r.productionUnit}`)}</td><td><button onClick={() => setEditing(r)}>Editar</button></td></tr>)}</tbody></table></div></section></>;
  }
  if (tab === "legacy") {
    const legacy = data.legacy;
    if (!legacy) return <section className="detailCard"><h2>Dashboard histórico</h2><p>Importe a base v2 preparada com a planilha histórica para consultar todos os indicadores.</p></section>;
    const selected = legacy.rows.filter(r => legacyId === "all" || r.rowId === legacyId);
    const total = (key: string) => { const values = selected.map(r => r.metrics[key]); return values.every(v => v != null) ? values.reduce<number>((s, v) => s + v!, 0) : null; };
    const key = total("keyHours"), work = total("workHours"), distance = total("distanceKm"), forward = total("forwardHours"), reverse = total("reverseHours");
    const percentage = (n: number | null, d: number | null) => n === null || !d ? null : n / d * 100;
    return <><section className="detailCard"><h2>Dashboard histórico · agosto de 2023</h2><p>Visão geral e individual preservadas a partir das abas de origem. Esta base agregada fica separada da série diária atual: períodos, localidade e granularidade não são comparáveis automaticamente.</p><label>Visão individual por cartão / linha<select value={legacyId} onChange={e => setLegacyId(e.target.value)}><option value="all">Visão geral das linhas da origem</option>{legacy.rows.map(r => <option key={r.rowId} value={r.rowId}>{r.cardCode === null ? "Cartão incompleto" : `Cartão ${r.cardCode}`} · linha {r.sourceRow}{r.cardQuality === "ambiguous" ? " · código ambíguo" : ""}</option>)}</select></label><section className="kpiGrid">{[["Distância", f(distance, " km")], ["Chave ligada", f(key, " h")], ["Trabalho/chave", f(percentage(work, key), "%")], ["Distância/hora de chave", f(distance !== null && key ? distance / key : null, " km/h")]].map(([label, value]) => <article className="kpiCard" key={label}><div><span>{label}</span><strong>{value}</strong></div></article>)}</section><p>Frente: {f(percentage(forward, forward !== null && reverse !== null ? forward + reverse : null), "%")} · Ré: {f(percentage(reverse, forward !== null && reverse !== null ? forward + reverse : null), "%")}. Razões gerais recalculadas pelas somas, sem média simples de percentuais.</p><div className="hysterTable"><table><thead><tr><th>Indicador original</th><th>Total selecionado</th></tr></thead><tbody>{Object.entries(metricLabels).map(([key, label]) => <tr key={key}><th>{label}</th><td>{f(total(key))}</td></tr>)}</tbody></table></div>{selected.length === 1 && <details><summary>Demais indicadores calculados da aba Base dados</summary><p>Valores em cache preservados; percentuais aparecem como razões (0,76 = 76%). Valores negativos e erros precisam de revisão da fórmula de origem.</p><div className="hysterTable"><table><tbody>{Object.entries(selected[0].legacyCalculated).map(([label, value]) => <tr key={label}><th>{label}</th><td>{f(value)}</td></tr>)}</tbody></table></div></details>}{selected.length === 1 && selected[0].legacyDisplay && <details><summary>Quadro original · Auxiliar Oficial</summary><p>Valores estáticos preservados, incluindo impactos e grupo. Não são recalculados nem associados aos eventos de 2022.</p><div className="hysterTable"><table><tbody>{Object.entries(selected[0].legacyDisplay).map(([label, value]) => <tr key={label}><th>{label}</th><td>{typeof value === "number" ? f(value) : value}</td></tr>)}</tbody></table></div></details>}</section><section className="detailCard"><h2>Conciliação do histórico</h2><ul>{legacy.warnings.map(w => <li key={w}>{w}</li>)}</ul><p>Impactos no quadro estático: {legacy.displayImpactTotal}, sem conciliação com o arquivo de eventos. Eventos arquivados em 2022: {legacy.impactArchive.length}. Combustível exibido no quadro estático: {f(legacy.displayFuelKg)} kg, não validado.</p><small>Fonte: {legacy.sourceFile} · {legacy.periodLabel} · SHA-256 {legacy.sha256}</small></section></>;
  }
  return null;
}
